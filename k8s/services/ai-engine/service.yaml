apiVersion: v1
kind: Service
metadata:
  name: ai-engine-service
  labels:
    app: ai-engine-service
spec:
  selector:
    app: ai-engine-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
