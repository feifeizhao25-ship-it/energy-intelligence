apiVersion: v1
kind: Service
metadata:
  name: diagnostic-service
  labels:
    app: diagnostic-service
spec:
  selector:
    app: diagnostic-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
