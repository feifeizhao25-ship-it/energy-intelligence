apiVersion: v1
kind: Service
metadata:
  name: resource-service
  labels:
    app: resource-service
spec:
  selector:
    app: resource-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
