apiVersion: v1
kind: Service
metadata:
  name: simulation-service
  labels:
    app: simulation-service
spec:
  selector:
    app: simulation-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
