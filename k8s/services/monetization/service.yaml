apiVersion: v1
kind: Service
metadata:
  name: monetization-service
  labels:
    app: monetization-service
spec:
  selector:
    app: monetization-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
