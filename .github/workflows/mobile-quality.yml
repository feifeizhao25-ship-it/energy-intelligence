name: Mobile Quality

permissions:
  contents: read

on:
  push:
    branches: [main, develop]
    paths:
      - 'android-cn/**'
      - 'ios-cn/**'
      - 'android-global/**'
      - 'ios-global/**'
      - 'scripts/verify_language_isolation.py'
      - 'scripts/gradle-cn-mirrors.init.gradle'
      - '.github/workflows/mobile-quality.yml'
  pull_request:
    paths:
      - 'android-cn/**'
      - 'ios-cn/**'
      - 'android-global/**'
      - 'ios-global/**'
      - 'scripts/verify_language_isolation.py'
      - 'scripts/gradle-cn-mirrors.init.gradle'
      - '.github/workflows/mobile-quality.yml'

jobs:
  language:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/verify_language_isolation.py

  flutter:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        edition: [android-cn, ios-cn, android-global, ios-global]
    defaults:
      run:
        working-directory: ${{ matrix.edition }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Android build JDK
        if: startsWith(matrix.edition, 'android-')
        uses: actions/setup-java@v4
        with:
          # Azure Zulu avoids the TLS handshake regression observed with the
          # current hosted Temurin image while resolving Gradle Plugin Portal.
          distribution: zulu
          java-version: '17'
          cache: gradle
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: stable
          cache: true
      - name: Configure resilient Gradle repositories
        if: startsWith(matrix.edition, 'android-')
        run: echo "Android builds use the verified Aliyun mirrors first and retain official repositories as fallbacks."
      - run: flutter pub get
      - run: flutter analyze --no-fatal-infos --no-fatal-warnings
      - run: flutter test --no-pub
      - name: Build Android release smoke artifact
        if: startsWith(matrix.edition, 'android-')
        env:
          ALLOW_DEBUG_RELEASE_SIGNING: "true"
          USE_CN_MAVEN_MIRRORS: "true"
        run: |
          for attempt in 1 2 3; do
            flutter build apk --release --no-pub && exit 0
            if [ "$attempt" -eq 3 ]; then
              exit 1
            fi
            echo "Gradle dependency download failed; retrying attempt $((attempt + 1))/3 after 15 seconds"
            sleep 15
          done
