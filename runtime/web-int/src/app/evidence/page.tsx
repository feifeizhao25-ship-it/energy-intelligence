const evidence=[['Resource series','Satellite-derived historical screening data','Updated when a new assessment is run'],['Tariff assumption','User-entered or documented market reference','Requires local commercial review'],['Calculation version','Versioned formula and assumption set','Stored with each auditable result'],['Uncertainty note','Sensitivity around yield, price, and losses','Shown beside—not hidden behind—the result']];
export default function Evidence(){return <main className="shell"><div className="eyebrow">Evidence and provenance</div><h1>Know what the answer is built on.</h1><p>Every decision output should distinguish measured facts, external references, user assumptions, and model-generated interpretation.</p><div className="grid two">{evidence.map(([name,source,note])=><article className="card" key={name}><span className="badge">Traceable</span><h2>{name}</h2><p>{source}</p><p>{note}</p></article>)}</div></main>}
