'use client';import{useState}from'react';
export default function Assessment(){const[capacity,setCapacity]=useState(1000);const[resource,setResource]=useState(1500);const generation=Math.round(capacity*resource*.82);return <main className="shell"><div className="eyebrow">Screening assessment</div><h1>Test the assumptions before the investment case.</h1><div className="grid two"><section className="card stack"><label>Technology<select><option>Solar PV</option><option>Onshore wind</option><option>Battery storage</option></select></label><label>Capacity (kW)<input type="number" min="1" value={capacity} onChange={e=>setCapacity(Number(e.target.value))}/></label><label>Annual resource reference (kWh/m²)<input type="number" min="1" value={resource} onChange={e=>setResource(Number(e.target.value))}/></label></section><section className="card"><span className="badge">Illustrative screening result</span><div className="metric">{generation.toLocaleString('en-US')}</div><p>kWh estimated annual generation using an explicit 0.82 system factor.</p><p>This is not a bankable yield report. Validate loss assumptions, measurement period, curtailment, degradation, and local grid constraints.</p></section></div></main>}
